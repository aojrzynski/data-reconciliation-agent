# Interpreting Outputs

Planned output artifact types:

- JSON trace: machine-readable run evidence
- markdown report: human-readable summary
- exception CSVs: concrete mismatch records

Deterministic outputs are canonical. Optional LLM polish can improve readability later but does not override deterministic results.
